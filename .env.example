# UDE+ Environment Variables
# Copy this file to .env.local and fill in the values for your environment

# Server Configuration
# The port on which the development server runs (default: 5173)
VITE_PORT=5173

# The port for the backend API server (default: 3000)
PORT=3000

# WebSocket / Real-time Communication
# Optional: Message to send as a ping/heartbeat to keep connections alive
# PING_MESSAGE=ping

# Development / Production
# Set to 'development' for detailed logging, 'production' for minimal logging
NODE_ENV=development

# Feature Flags (Optional)
# Enable or disable features for testing
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_EXPERIMENTAL_FEATURES=false

# Database Configuration (if applicable)
# DATABASE_URL=

# Authentication (if using external auth provider)
# AUTH_SECRET=
# AUTH_PROVIDER=

# API Configuration
# Base URL for API calls (usually the same as the backend server)
VITE_API_URL=http://localhost:3000

# Deployment Configuration
# For Netlify deployment
# NETLIFY_SITE_ID=
# NETLIFY_AUTH_TOKEN=

# For Vercel deployment
# VERCEL_PROJECT_ID=
# VERCEL_TOKEN=

# Monitoring & Analytics (Optional)
# SENTRY_DSN=
# GOOGLE_ANALYTICS_ID=

# Notes:
# - Keep this file in version control but never commit actual secrets
# - Use .env.local for local development overrides (add to .gitignore)
# - For production, set environment variables in your deployment platform's settings
# - All VITE_* variables are exposed to the browser (don't store secrets there)
# - Variables without VITE_ prefix are only available on the server side
